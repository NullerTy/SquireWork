generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://Authentication_owner:bYIsiWO67vCR@ep-shy-snowflake-a2q80yt3.eu-central-1.aws.neon.tech/Authentication?sslmode=require"
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
}
